import { Meta, Canvas } from '@storybook/blocks'
import * as TeamStories from './teams.stories'

<Meta title="Teams/Cameras" />

# Cameras

**Surface:** Camera grid, live view chrome, device health badges

**Example migration:** Status pill colors on camera tiles

**Mapping:** `--color-text-secondary → --text/secondary`

<Canvas of={TeamStories.Cameras} />
