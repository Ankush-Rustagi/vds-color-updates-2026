import { Meta } from '@storybook/blocks'
import { TokenExplorer } from '../../src/components/reference/TokenExplorer'
import { TOKEN_META } from '../../src/tokens/collection'

<Meta title="Reference/Effects" parameters={{ docs: { toc: false } }} />

# Effects

Blur, opacity, and scrim tokens from Figma Collection ({TOKEN_META.counts.effects} tokens).

<TokenExplorer dataset="effects" />
